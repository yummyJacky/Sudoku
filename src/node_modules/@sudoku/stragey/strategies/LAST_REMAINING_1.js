export const LAST_REMAINING_1 = {
    name: 'last remaining',
    description: '从未使用的数字中选择一个',
    // 这里必修返回一个bool值
    check: () => {
        return true
    },
    effect: (grid) => {
        console.log('使用最后剩余策略', grid)

        return true
    }
}